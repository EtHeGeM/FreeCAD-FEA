# FreeCAD FEA - MECT2222 Combined Loading Project

This repository is being prepared with the FreeCAD/CalculiX FEA project report, scripts, model files, solver outputs, and study notes.

A full local commit has been prepared at `/tmp/FreeCAD-FEA-upload`, but direct `git push` from this environment needs GitHub credentials.
